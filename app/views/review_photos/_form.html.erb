<%= form_for(@review_photo) do |f| %>
  <% if @review_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review_photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @review_photo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
