<p>走马灯照片</p>
<table class="table">
  <thead>
    <tr>
      <th>Thumb</th>
      <th>Show</th>
      <th>Edit</th>
      <th>Destroy</th>
    </tr>
  </thead>

  <% photos.each do |photo| %>
      <tr>
        <td><%= image_tag photo.photofile.url(:thumb) %></td>
        <td><%= link_to 'Show', photo %></td>
        <td><%= link_to 'Edit', edit_photo_path(photo) %></td>
        <td><%= link_to 'Destroy', photo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
</table>